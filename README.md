# General info
Social is a social application built with React.

# Setup
To run this project, install it locally using npm:

```
npm install
npm start
```
## Technologies Used
- React
- SCSS
- Firebase
